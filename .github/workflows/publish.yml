name: CI

on:
  pull_request:
  push:
    branches:
      - master
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  create-release:
    name: Publish all packages to npm on tag
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      # Work around the broken npm bundled with Node 22.22.2 on GitHub runners.
      - name: Upgrade npm (Trusted Publishing requires 11.5.1+)
        run: npx -y npm@11 install -g npm@11

      - name: Install dependencies
        run: npm ci

      - name: Publish to npm with provenance
        run: npm run release:all
        env:
          PROVENANCE_FLAG: '--provenance'

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generate_release_notes: true
          body: |
            Published 5 packages to npm:

            - [`n8n-nodes-agent-brains-trigger@${{ github.ref_name }}`](https://www.npmjs.com/package/n8n-nodes-agent-brains-trigger/v/${{ github.ref_name }})
            - [`n8n-nodes-agent-brains@${{ github.ref_name }}`](https://www.npmjs.com/package/n8n-nodes-agent-brains/v/${{ github.ref_name }})
